# CLI 命令行工具

`td` 是 Typedown 的命令行入口。

## 常用命令

*   `td init <project_name>`: 初始化一个新的 Typedown 项目结构。
*   `td check`: 运行完整的分析和验证流程，报告错误。
*   `td build --output <dir>`: 编译文档并输出静态站点（待定）。
*   `td materialize [file_path]`: 将指定文件（或全部文件）中的“语法糖”展开为完整代码块。

## 参数说明

使用 `td --help` 查看所有可用选项。
